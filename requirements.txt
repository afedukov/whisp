# Core dependencies - faster-whisper
# Note: faster-whisper requires ctranslate2 and its dependencies
ctranslate2>=4.0.0
tokenizers>=0.13.0
huggingface-hub>=0.13.0
faster-whisper>=1.0.0

# Audio processing
soundfile>=0.12.1
numpy>=1.24.0

# Audio recording (for record mode)
sounddevice>=0.4.6

# CLI and UI
rich>=13.0.0
simple-term-menu>=1.6.0



# Configuration
pyyaml>=6.0
